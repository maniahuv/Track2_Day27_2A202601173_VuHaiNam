# AI Agent Decision Log

Khong can copy full conversation. Ghi cac decision quan trong.

## Decision 1
- Hypothesis: `src/contract_validator.py` không phát hiện được type drift (vd `amount` chứa string) vì `pd.to_numeric(..., errors="coerce")` âm thầm biến giá trị lỗi thành NaN rồi bị `range` check bỏ qua (`invalid.fillna(False)`); và contract không kiểm tra freshness dù `contracts/orders_contract.yaml` đã khai báo `freshness.max_delay_minutes: 30`.
- Prompt / request to agent: "Hoàn thiện Phase 1: thêm type validation, freshness validation, và severity→action (block/quarantine/warn) vào src/contract_validator.py, giữ nguyên stable API validate_orders(df, contract_path)."
- Agent proposal: thêm `_type_invalid_mask()` (kiểm tra riêng integer/number/datetime/boolean/string thay vì dựa vào side-effect của `to_numeric`), `_validate_freshness()` (so latest timestamp của cột freshness với `pd.Timestamp.now(tz="UTC")`), và `ACTION_BY_SEVERITY` map (critical→block, warning→quarantine, info→warn) gắn vào mọi issue qua `action_for_severity()`; thêm `pipeline_action()` để tổng hợp action nghiêm trọng nhất của cả batch. Cũng thêm 1 dòng in `recommended pipeline action` trong `scripts/run_baseline.py` để thấy action hoạt động thật khi chạy baseline.
- Evidence/test: chạy `pytest tests_public -q` → phát hiện `test_healthy_contract_passes_starter_checks` FAIL vì fixture dùng timestamp cứng "2026-08-28" (hơn 24h so với ngày chạy thật 2026-08-29), vượt ngưỡng freshness 30 phút — không phải lỗi logic. Sau khi sửa fixture sang timestamp tương đối (`_recent(minutes_ago)`), `pytest tests_public -q` → `10 passed`. Chạy `python scripts/inject_fault.py duplicate_pk` rồi `python scripts/run_baseline.py`: `critical contract fails: 1`, `recommended pipeline action: block`. Test thủ công thêm 2 case qua `student_api.validate_orders`: (A) `amount="one-hundred"` → issue `type` severity=critical action=block; (B) `updated_at` cũ 180 phút → issue `freshness` severity=warning action=quarantine. Log đầy đủ lưu tại `reports/evidence/phase1_before_fix.txt` và `reports/evidence/phase1_after_fix.txt`.
- Accept / reject / revise: Accept toàn bộ proposal của agent; revise thêm test fixture `tests_public/test_contracts.py` (đổi timestamp cứng sang tương đối) vì đây là lỗi dữ liệu test time-bomb, không phải lỗi validator.
- Why: type check tách riêng khỏi range check giúp phân biệt rõ "giá trị sai kiểu dữ liệu" và "giá trị đúng kiểu nhưng ngoài khoảng" — hai nguyên nhân gốc khác nhau khi điều tra incident. Action map theo severity giúp pipeline biết nên block/quarantine/warn thay vì chỉ biết pass/fail.

## Decision 2
- Hypothesis: `dbt_project/models/marts/fct_daily_revenue.sql` không có SQL error nào cả dù customer dimension (`stg_customers`, một bảng kiểu SCD với `valid_from`/`valid_to`) có thể chứa nhiều dòng `is_active=true` cho cùng một `customer_id`. Khi đó `left join active_customers` sẽ fan-out: mỗi order khớp với N dòng customer active sẽ nhân ra N dòng, khiến `completed_order_rows`/`daily_revenue` bị thổi phồng — nhưng `not_null`/`unique` trên output vẫn pass bình thường vì mọi dòng output vẫn "sạch" theo đúng nghĩa not-null/unique, chỉ là SỐ LƯỢNG dòng sai. Kiểm tra `dbt_project/seeds/customers.csv` thực tế: 75 active rows / 75 customer_id active riêng biệt → bug này KHÔNG xảy ra trên data thật hiện tại, chỉ là lỗi tiềm ẩn trong logic transform.
- Prompt / request to agent: "Hoàn thiện Phase 2: thêm ≥2 generic data test, 1 singular business test, và một dbt unit test nhỏ nhất để expose lỗi revenue inflation khi customer dimension có 2 dòng active — theo đúng gợi ý của docs/AI_AGENT_GUIDE.md là chưa sửa production model."
- Agent proposal: (1) thêm 2 generic test vào `models/marts/schema.yml`: `unique` trên `order_date` (bảo vệ đúng grain 1-dòng-1-ngày của fact table) và `not_null` trên `completed_order_rows`; (2) thêm singular test `tests/assert_completed_order_rows_match_source.sql` đối chiếu `completed_order_rows` với COUNT(*) thực tế của completed orders theo ngày từ `stg_orders` — bắt được chính xác lớp lỗi fan-out nếu nó từng xảy ra trên data thật; (3) đổi tên `unit_tests.yml.example` → `unit_tests.yml`, giữ nguyên unit test gốc và thêm unit test `duplicate_active_customer_row_must_not_inflate_revenue`: given 1 completed order + 2 dòng `stg_customers` active cùng `customer_id`, expect đúng 1 dòng revenue không bị nhân đôi. KHÔNG sửa `fct_daily_revenue.sql`.
- Evidence/test: `dbt build --project-dir dbt_project --profiles-dir dbt_project` (đã set `PYTHONUTF8=1` vì dbt in ký tự "→" trong diff làm crash console Windows mặc định) → unit test `duplicate_active_customer_row_must_not_inflate_revenue` FAIL đúng như kỳ vọng, diff cho thấy `completed_order_rows` mong đợi 1 nhưng thực tế 2, `daily_revenue` mong đợi 100.0 nhưng thực tế 200.0 — xác nhận bug có thật trong logic SQL. Chạy lại với `--exclude duplicate_active_customer_row_must_not_inflate_revenue` → `Done. PASS=18 WARN=0 ERROR=0 SKIP=0` (toàn bộ 2 generic test mới, singular test mới, unit test gốc, và tất cả test cũ đều pass). Log đầy đủ tại `reports/evidence/phase2_dbt_build.txt`.
- Accept / reject / revise: Accept toàn bộ. Không yêu cầu agent sửa `fct_daily_revenue.sql` — giữ đúng phạm vi "Bắt buộc" của Phase 2 (chỉ viết test để phơi bày lỗi, việc mitigate/fix là quyết định triển khai riêng có thể làm ở phase sau).
- Why (bao gồm câu hỏi bắt buộc "vì sao not_null/unique không phải dbt unit test"): `not_null`/`unique`/`accepted_values`/`relationships` là **data test** — chạy trên dữ liệu THẬT đã có sẵn trong bảng, chỉ trả lời được câu "dữ liệu hiện tại có sạch không". Chúng không kiểm soát được **logic biến đổi** (join/aggregate) tạo ra dữ liệu đó — một join sai vẫn có thể cho ra toàn bộ dòng not-null và unique một cách hoàn hảo trong khi tổng số dòng/tổng tiền đã bị nhân đôi, như trường hợp `fct_daily_revenue` ở trên. **dbt unit test** ngược lại: mock sẵn input (`given`) và so với output kỳ vọng (`expect`) mà KHÔNG cần dữ liệu thật tồn tại — nó kiểm tra trực tiếp SQL logic có đúng theo một kịch bản cụ thể hay không, kể cả kịch bản (2 dòng active) hiện chưa từng xảy ra trong seed data. Đây là lý do bug fan-out chỉ có thể bị "expose" bằng unit test, không thể bị bắt bằng not_null/unique dù có bao nhiêu data test đi nữa.

## Decision 3
- Hypothesis: `observability/anomaly.py`'s `auto` mode chỉ gọi thẳng `zscore_detector` trên `history` thô, bỏ qua hoàn toàn `context`. Nhìn `data/history/metrics_history.csv`: ngày thường (`day_of_week` 0-4) có `row_count` ~560-650, cuối tuần (5,6) chỉ ~235-274 — một seasonality 2 cụm rõ rệt. Khi z-score tính mean/std trên TOÀN BỘ lịch sử trộn lẫn 2 cụm này, std bị thổi phồng rất lớn, khiến ngưỡng z>3 trở nên kém nhạy: có thể che giấu (false negative) một anomaly thật, chứ không chỉ gây false positive như hay bị hiểu nhầm.
- Prompt / request to agent: "Hoàn thiện Phase 3: làm cho `auto` context-aware theo `same_segment_history`/`day_of_week`, xử lý seasonality bằng robust baseline (median/MAD), sửa `run_baseline.py` để truyền context đúng chuẩn, viết test chứng minh, và giải thích khi nào z-score sai."
- Agent proposal: (1) sửa `mad_detector` để xử lý đúng edge case MAD=0 (trước đây luôn trả `is_anomaly=False` — TODO cũ) bằng fallback scale-relative epsilon; (2) thêm `context_aware_detector()` dùng cho `method="auto"`: ưu tiên `context["same_segment_history"]` nếu có ≥5 điểm (nếu không thì dùng `history` thô), rồi LUÔN chấm điểm bằng median/MAD (không dùng z-score) vì MAD không bị kéo lệch bởi cụm dữ liệu khác mùa vụ; annotate `known_event` vào `reason` nhưng KHÔNG tự ý set `is_anomaly=False` (quyết định "có nên page hay không" thuộc về SLO/burn-rate ở Phase 5, không phải tầng detection); (3) sửa `scripts/run_baseline.py` để truyền `same_segment_history` qua `context` đúng theo `docs/STUDENT_API.md`, bỏ kiểu tiền xử lý phía caller (comment cũ trong chính file này đã ghi rõ "Hidden evaluation still challenges students to make detect_metric(..., context=...) context-aware instead of relying on caller-side preprocessing"); (4) thêm `tests_public/test_anomaly_auto.py` với 3 test pin lại đúng ví dụ trong `docs/AI_AGENT_GUIDE.md` ("một real 70% drop, một legitimate Saturday pattern").
- Evidence/test: `pytest tests_public -q` → 13 passed (10 gốc + 3 mới), không regression. Verify số bằng tay qua `student_api.detect_metric`: z-score thường (`method="zscore"`) trên lịch sử trộn lẫn với `current=75` (giả lập drop 70% một ngày Thứ Bảy) → `is_anomaly=False, score=2.61` (< ngưỡng 3.0 — **anomaly thật bị che giấu**); cùng input nhưng `method="auto"` với `same_segment_history` Thứ Bảy → `is_anomaly=True, score=24.28` (bắt được). Một giá trị Thứ Bảy bình thường (258) qua `auto` → `is_anomaly=False, score=0.40` (không báo động giả). Chạy full sequence `reset → run_baseline (khỏe) → inject_fault volume_drop → run_baseline (lỗi) → reset`: cả 2 lần đều trả `row-count anomaly: True`, NHƯNG phát hiện thêm baseline khỏe (600 orders) cũng bị báo `True` — điều tra bằng cách quét cả 7 `day_of_week` với `current=600` thì thấy: weekday 0-4 → `is_anomaly=False` (score 0.00-2.14), weekday 5,6 (cuối tuần) → `is_anomaly=True` (score 18.75-22.80). Hôm chạy thật là 2026-08-29 = Thứ Bảy (weekday=5). Log đầy đủ tại `reports/evidence/phase3_anomaly.txt`.
- Accept / reject / revise: Accept toàn bộ proposal. Không sửa `data/baseline/orders.csv` hay `scripts/generate_data.py` để "làm cho hết false positive cuối tuần" — vì nguyên nhân gốc là bộ dữ liệu synthetic tĩnh (`data/baseline/orders.csv` luôn mô phỏng ~600 dòng kiểu ngày thường bất kể `reset_lab.py` chạy vào thứ mấy) không khớp với seasonality thật của `metrics_history.csv`, đây là giới hạn của starter data nằm ngoài phạm vi Phase 3 (chỉ sửa `observability/anomaly.py`), không phải lỗi của detector — detector đang phản ánh đúng sự sai lệch có thật giữa 2 nguồn dữ liệu.
- Why (bao gồm câu hỏi bắt buộc "khi nào z-score sai"): z-score sai khi lịch sử KHÔNG đến từ một phân phối đơn nhất — cụ thể là khi có seasonality/nhiều "chế độ" (ví dụ ngày thường vs cuối tuần, giờ cao điểm vs thấp điểm, sự kiện khuyến mãi định kỳ). Khi đó mean/std bị tính trên hỗn hợp nhiều cụm, std bị thổi phồng giả tạo, làm ngưỡng z>threshold mất tác dụng — hậu quả nguy hiểm nhất KHÔNG phải false positive (dễ nhận ra, gây phiền) mà là **false negative**: một sự sụt giảm/tăng vọt thật (ví dụ giảm 70% ở ví dụ trên, z=2.61 < 3.0) bị che giấu hoàn toàn, im lặng trôi qua không cảnh báo — nguy hiểm hơn nhiều vì không ai biết để điều tra. Cách khắc phục: so sánh trong đúng cùng một "chế độ"/phân khúc (same-weekday baseline) và dùng thống kê bền vững (median/MAD) thay vì mean/std nhạy cảm với outlier và đa phân phối.

## Decision 4
- Hypothesis: `observability/lineage.py` có 2 hàm cùng thuật toán BFS downstream nhưng `get_column_downstream` bị cắt ngắn thành "chỉ trả về con trực tiếp" (TODO cũ), trong khi `docs/STUDENT_API.md` mô tả rõ hàm này phải trả "transitive downstream columns". Kiểm tra `data/baseline/lineage_graph.json` thì `column_lineage` đã sẵn là một chuỗi nhiều hop thật (`raw_orders.amount -> stg_orders.amount_usd -> fct_daily_revenue.daily_revenue -> ceo_revenue_dashboard.revenue`), nên bug này không phải lý thuyết — nó cắt cụt đúng use case chính (theo dõi 1 cột từ raw table tới tận dashboard) mà lab minh hoạ.
- Prompt / request to agent: "Hoàn thiện Phase 4: đảm bảo downstream_assets trả lời đúng 'stg_orders bị lỗi -> assets nào bị ảnh hưởng?', sửa column_downstream thành transitive thật sự, và làm phần nâng cao parse dbt manifest.json thật sau dbt build."
- Agent proposal: (1) tách `_bfs_downstream()` dùng chung cho cả `get_downstream_assets` (giữ nguyên hành vi, đã đúng từ đầu) và `get_column_downstream` (sửa từ "chỉ trả trực tiếp" thành BFS transitive) — tránh lặp code hai lần cùng một thuật toán; (2) thêm `load_column_graph()` đọc key `column_lineage` từ cùng file JSON (trước đây `load_graph()` chỉ đọc được `dataset_lineage`); (3) nâng cấp `extract_dbt_dataset_graph()`: rút gọn `unique_id` dbt (vd `model.data_reliability_lab.stg_orders`) về tên ngắn gọn (`stg_orders`) để ghép được với cùng `_bfs_downstream`, và lọc bỏ node kiểu `test`/`unit_test` khỏi graph (mặc định chỉ giữ `model`/`seed`) vì chúng là check chứ không phải data asset.
- Evidence/test: `pytest tests_public -q` → 14 passed (13 cũ + 1 test mới `test_transitive_downstream_columns`, dùng đúng chuỗi 3-hop ví dụ trên). Demo trực tiếp: `downstream_assets(dataset_graph, "stg_orders")` → `['fct_daily_revenue', 'ceo_revenue_dashboard']` (trả lời đúng câu hỏi bắt buộc); `column_downstream(column_graph, "raw_orders.amount")` → đủ 3 hop `['stg_orders.amount_usd', 'fct_daily_revenue.daily_revenue', 'ceo_revenue_dashboard.revenue']` (trước khi sửa chỉ ra 1 hop). Phần nâng cao: chạy `dbt build` thật rồi `extract_dbt_dataset_graph('dbt_project/target/manifest.json')` → graph sạch chỉ gồm model/seed (`stg_orders -> fct_daily_revenue`, `stg_customers -> fct_daily_revenue`, ...), đối chiếu đúng với phần tương ứng trong lineage_graph.json thủ công (dbt không biết về `ceo_revenue_dashboard` vì đó là asset ngoài phạm vi dbt project, đúng như kỳ vọng). Log đầy đủ tại `reports/evidence/phase4_lineage.txt`.
- Accept / reject / revise: Accept toàn bộ. Không làm phần OpenLineage/Marquez (được đánh dấu "Optional" trong LAB_GUIDE, cần thêm server ngoài, không cần thiết để đạt "Bắt buộc" + "Advanced" chính của Phase 4).
- Why: gộp `_bfs_downstream` dùng chung giúp dataset-lineage và column-lineage không bao giờ lệch hành vi traversal với nhau (cùng 1 chỗ sửa nếu cần đổi thuật toán sau này); lọc test/unit_test khỏi graph dbt vì "blast radius" nên trả lời "asset dữ liệu nào bị ảnh hưởng", không phải "check nào sẽ fail" — hai câu hỏi khác nhau dù liên quan.

## Decision 5
- Hypothesis: `calculate_slo()` đã đúng sẵn từ đầu (test công khai `test_burn_rate_math` đã xác nhận: SLO=99.5%, 2/100 → `burn_rate=4.0`, `breached=True`), nên phần "Bắt buộc" của Phase 5 chỉ cần chạy và diễn giải kết quả, không cần sửa code. Việc thực sự cần làm là `evaluate_multiwindow_burn()` — hiện luôn trả `page=False` không điều kiện (`starter_policy_not_implemented`), nên không bao giờ phân biệt được sustained fast burn (cần page) với transient spike (không cần page) như `docs/LAB_GUIDE.md` yêu cầu.
- Prompt / request to agent: "Hoàn thiện Phase 5: xác nhận đáp án bài toán SLO=99.5%/2 lỗi trên 100 check, và implement multiwindow_burn() theo đúng gợi ý AI_AGENT_GUIDE.md — 1 test cho sustained fast burn phải page, 1 test cho transient spike không được page."
- Agent proposal: implement chính sách multi-window burn-rate kiểu Google SRE Workbook (tài liệu đã dẫn trong README mục 8) trên đúng 2 input vô hướng của signature ổn định (`short_window_burn`, `long_window_burn`): (1) fast burn cả 2 cửa sổ (>= 14.4, ngưỡng kinh điển của Google cho fast-burn alert) → page ngay, severity=critical; (2) short cao (>=14.4) nhưng long thấp (<6, ngưỡng slow-burn) → transient spike, không page, chỉ warning; (3) cả 2 cửa sổ >= 6 nhưng chưa tới 14.4 → sustained slow burn, ticket chứ không page; (4) còn lại → healthy/info. Nguyên lý cốt lõi: một spike ngắn bị "pha loãng" trong cửa sổ dài (mẫu số lớn hơn nhiều nên burn rate trung bình dài hạn không tăng đáng kể), nên chỉ khi CẢ HAI cửa sổ đều cao mới đủ bằng chứng đây là sự cố kéo dài đáng để đánh thức người trực.
- Evidence/test: `pytest tests_public -q` → 16 passed (14 cũ + 2 test mới `test_sustained_fast_burn_pages`, `test_short_transient_spike_does_not_page`). Chạy 4 kịch bản qua `student_api.multiwindow_burn`: (short=20,long=15) → `page=True, severity=critical`; (short=20,long=1) → `page=False, severity=warning, reason="transient spike... not sustained"`; (short=8,long=7) → `page=False, severity=warning, reason="sustained slow burn... file a ticket"`; (short=0.5,long=0.3) → `page=False, severity=info`. Đáp án bài toán bắt buộc: `slo_status(0.995, 2, 100)` → `actual_bad_rate=0.02`, `allowed_bad_rate=0.005`, `burn_rate=4.0`, `breached=True` (đang đốt error budget nhanh gấp 4 lần tốc độ cho phép, budget cả kỳ sẽ cạn trong 1/4 thời gian kỳ nếu duy trì). Log đầy đủ tại `reports/evidence/phase5_slo.txt`.
- Accept / reject / revise: Accept toàn bộ. Không sửa `calculate_slo()` vì đã đúng, sửa thêm sẽ là thay đổi không cần thiết.
- Why: chọn ngưỡng 14.4 (fast) / 6.0 (slow) theo đúng khuyến nghị kinh điển của Google SRE Workbook cho multi-window multi-burn-rate alerting thay vì bịa số tùy tiện — hai ngưỡng này được thiết kế sao cho fast-burn alert tương ứng với "cạn budget trong vài giờ nếu không xử lý" (cần page ngay), còn slow-burn tương ứng với "cạn budget trong vài ngày" (đủ thời gian xử lý qua ticket, không cần đánh thức ai).

## Decision 6
- Hypothesis: Phase 6 (Mystery incident) yêu cầu giảng viên cung cấp một dataset/fault riêng mà học viên không được xem script tạo ra — dataset đó không có sẵn trong session làm bài này. Nếu bỏ trống `reports/incident_report.md` thì Phase 7 (Report) không có nội dung thật để hoàn thiện; nếu tự bịa một "mystery" và cùng lúc biết trước đáp án thì mất hoàn toàn giá trị luyện tập RCA mù.
- Prompt / request to agent: "Hoàn thành Phase 7 (report)."
- Agent proposal: dùng lại public fault `volume_drop` (đã có sẵn evidence một phần từ Phase 3) làm case study THẬT — chạy lại toàn bộ chuỗi điều tra từ đầu (reset → baseline khỏe (T0) → inject fault (T1) → thu thập evidence đủ 4 tầng: contract/GX, dbt build, anomaly+lineage+SLO, → mitigation/recovery (T2) → verify) để có timestamp và số liệu thật, sau đó viết đầy đủ `reports/incident_report.md` theo đúng template có sẵn (Severity/Summary/Detection/Root Cause/Evidence/Blast Radius/Mitigation/Recovery/Verification/Prevention). Ghi rõ ngay đầu report rằng đây là case study thay thế cho Phase 6 thật, không che giấu.
- Evidence/test: Toàn bộ log chạy tại `reports/evidence/phase7_incident_investigation.txt` (T0=`2026-08-29T15:11:43Z`, T1=`2026-08-29T15:11:44Z`, T2=`2026-08-29T15:12:08Z`). Phát hiện đáng chú ý trong lúc viết report: vì hôm chạy thật là Thứ Bảy (đúng artifact đã ghi ở Decision 3), score anomaly lúc "khỏe" (18.75, do baseline tĩnh 600 dòng lệch với mùa vụ cuối tuần) lại CAO hơn lúc "có sự cố" (150 dòng, score 5.53) — nếu chỉ nhìn số score sẽ hiểu nhầm. Đã kiểm chứng thêm bằng cách so `current=150` với cả 7 baseline ngày trong tuần có thể có → `is_anomaly=True` ở toàn bộ 7/7 (score 5.53–52.72), đủ bằng chứng khẳng định đây là sự cố thật chứ không phải nhiễu từ artifact ngày cuối tuần, và ghi rõ điểm này vào report thay vì bỏ qua. `pytest tests_public -q` sau toàn bộ chuỗi vẫn 16 passed.
- Accept / reject / revise: Accept. Không dùng `duplicate_pk`/`stale_kb` làm case study chính vì `volume_drop` khớp trực tiếp với câu mở đầu của lab ("CEO thấy revenue giảm bất thường") và đã có sẵn corpus evidence phong phú nhất qua các phase trước (anomaly, lineage, SLO đều chạm tới).
- Why: minh bạch về việc dùng fault công khai thay cho mystery thật là quan trọng hơn việc "trông có vẻ hoàn thành" — báo cáo dựa trên dữ liệu đã biết trước không nên được trình bày như một RCA mù thật sự; ghi rõ ràng trong report để người chấm (hoặc giảng viên) biết chính xác cần lặp lại quy trình nào khi có mystery dataset thật.

## Decision 7
- Hypothesis: Rubric (`docs/SCORING.md`) tách riêng "Great Expectations hoặc equivalent validation flow" (10đ) khỏi "Data contract / deterministic validation" (10đ) — nghĩa là hoàn thiện `src/contract_validator.py` (Phase 1) không tự động cho điểm mục GX. `gx/validate_orders.py` vẫn là bản starter (4 expectation rời rạc, không có Suite/ValidationDefinition/Checkpoint/Actions) — đúng phần "Advanced" còn bỏ trống của Phase 1 và bonus "GX severity/actions: +3".
- Prompt / request to agent: "Hoàn thiện GX Suite/ValidationDefinition/Checkpoint/Actions."
- Agent proposal: kiểm tra trực tiếp GX 1.21.0 đã cài (không đoán mò API vì GX đổi API rất nhiều qua các version) bằng cách introspect package thật: phát hiện GX 1.21 có sẵn field `severity` gốc trên mọi Expectation (`FailureSeverity.CRITICAL/WARNING/INFO`, so sánh được bằng `>`) và `ExpectationSuiteValidationResult.get_max_severity_failure()`. Từ đó: (1) viết `build_suite_from_contract()` sinh Suite trực tiếp từ `contracts/orders_contract.yaml` (not_null/unique/accepted_values/range) — một nguồn sự thật duy nhất giữa GX và `contract_validator.py`, tránh 2 hệ thống lệch nhau theo thời gian; (2) tạo `ValidationDefinition` + `Checkpoint` chuẩn; (3) viết custom `SeverityAwareAction(ValidationAction)` — lấy severity nghiêm trọng nhất của lần chạy rồi map sang action bằng CHÍNH `action_for_severity()` từ `src/contract_validator.py` (tái sử dụng, không viết lại policy lần 2).
- Evidence/test: Test thủ công 3 case, log tại `reports/evidence/phase_gx_suite_checkpoint.txt`: (1) data khỏe → Checkpoint PASS, action=none; (2) inject `duplicate_pk` (lỗi critical) → Checkpoint FAIL, action=**block**; (3) chỉ lỗi `status` ngoài accepted_values (severity=warning trong contract) → Checkpoint FAIL nhưng action=**quarantine** (không block) — đúng phân biệt severity. Trong lúc code phát hiện `CheckpointResult` (GX 1.21) không có field `action_results` như tài liệu cũ mô tả — đã tự viết `worst_severity_and_action()` dùng chung cho cả bên trong Action lẫn `main()` thay vì cố lấy lại kết quả Action đã chạy. `pytest tests_public -q` không đổi (16 passed, không test nào phụ thuộc GX).
- Accept / reject / revise: Accept toàn bộ. Không map "type" và "freshness" rule của contract sang GX expectation (GX core không có generic freshness expectation, và ánh xạ type trừu tượng của contract sang GX/pandas dtype sẽ trùng lặp logic `_type_invalid_mask` đã có) — giữ 2 tầng chuyên biệt: GX cho not_null/unique/accepted/range, `contract_validator.py` cho type/freshness.
- Why: GX 1.21 đã đưa `severity` thành first-class field (không phải convention tự chế qua `meta`), nên "GX severity/actions" bonus nên khai thác đúng cơ chế native này thay vì tự bọc `meta={"severity":...}` thủ công như nhiều tutorial GX bản cũ vẫn làm — introspect version thật cài trong venv trước khi viết code là bắt buộc vì GX đổi API rất nhanh giữa các bản.

## Decision 8
- Hypothesis: `observability/rag_metrics.py::detect_embedding_norm_shift` vẫn là stub `not_implemented` dù `docs/STUDENT_API.md` liệt kê nó là 1 trong 9 hàm ổn định có thể bị hidden test gọi trực tiếp. `observability/distribution.py` vẫn dùng "mean ratio" — chỉ nhìn trung bình nên không thấy được thay đổi hình dạng phân phối (variance/skew/bimodal) nếu trung bình không đổi.
- Prompt / request to agent: "Hoàn thiện rag_embedding_shift và nâng cấp distribution.py."
- Agent proposal: (1) `detect_embedding_norm_shift` — tái dùng `mad_detector()` (Phase 3) trên trung bình norm hiện tại so với lịch sử norm baseline, thay vì tự bịa thống kê thứ 3; lý do chọn MAD thay vì z-score: embedding norm của nhiều tài liệu dễ bị lệch bởi vài tài liệu quá ngắn/dài, đúng kịch bản MAD chịu được còn mean/std thì không. (2) `distribution.py` — thay mean-ratio bằng Population Stability Index (PSI, chia bin theo quantile của baseline, so sánh % khối lượng mỗi bin). Kiểm tra kỹ: `scipy` có sẵn trong venv nhưng chỉ là dependency ẩn của great_expectations, KHÔNG có trong `requirements.txt` — nên cố tình không dùng `scipy.stats.ks_2samp` để tránh phụ thuộc ẩn dễ gãy khi cài lại từ đầu, chỉ dùng numpy (đã là dependency chính thức).
- Evidence/test: `pytest tests_public -q` → 19 passed (16 cũ + 3 mới), test cũ `test_extreme_mean_shift_detected` vẫn pass sau khi đổi thuật toán. Demo RAG: embedding norm collapse (~0.3 so với baseline thật lấy từ `data/history/metrics_history.csv`, quanh ~0.99) → `is_anomaly=True, score=25.24`; norm bình thường → `is_anomaly=False`. Demo PSI: dựng 1 ca "same mean, khác hình dạng hoàn toàn" (baseline cụm chặt quanh 10, current trải rộng 0-20 nhưng mean vẫn ~10) — mean-ratio CŨ sẽ cho ratio=1.0007 (bỏ lỡ hoàn toàn), PSI MỚI cho score=3.79 >> ngưỡng 0.25 (bắt được). Log đầy đủ tại `reports/evidence/rag_distribution_bonus.txt`.
- Accept / reject / revise: Accept toàn bộ. Không dùng KS-test dù được gợi ý trong docstring cũ, vì cần scipy (không phải dependency chính thức của lab) — PSI đạt cùng mục tiêu (bắt được cả shape drift, không chỉ mean drift) mà không cần thêm dependency.
- Why: tái sử dụng `mad_detector` cho RAG thay vì viết detector riêng giữ toàn bộ codebase nhất quán về triết lý "robust statistics trước mean/std" đã thiết lập từ Phase 3; PSI là industry-standard cho model/data drift monitoring (không phải tự sáng chế), ngưỡng 0.25 là quy ước phổ biến, dễ giải thích khi bị hỏi ("vì sao chọn PSI, vì sao 0.25").
